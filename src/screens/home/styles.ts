import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
      flex: 1,
        backgroundColor: 'black',
        padding: 24,
    },
    eventName: {
        color: 'white',
        fontWeight: 'bold',
        marginTop: 48,
        fontSize: 24,
    },
    eventDate: {
      color: 'gray',
      fontSize: 16,
    }
  });